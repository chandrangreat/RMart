import { AddToCartDirective } from './add-to-cart.directive';
import { TestBed } from '@angular/core/testing';
import { ViewContainerRef } from '@angular/core';

describe('AddToCartDirective', () => {
  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [AddToCartDirective],
  //     providers: [ViewContainerRef],
  //   });
  // });
  // const viewContainerRef = TestBed.inject(ViewContainerRef);
  // it('should create an instance', () => {
  //   const directive = new AddToCartDirective(viewContainerRef);
  //   expect(directive).toBeTruthy();
  // });
  it('', () => {});
});
