import { ProductInfoDirective } from './product-info.directive';
import { TestBed } from '@angular/core/testing';
import { ViewContainerRef } from '@angular/core';

describe('ProductInfoDirective', () => {
  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ProductInfoDirective],
  //     providers: [ViewContainerRef],
  //   });
  // });
  // it('should create an instance', () => {
  //   const directive = new ProductInfoDirective(viewContainerRef);
  //   expect(directive).toBeTruthy();
  // });
  it('', () => {});
});
