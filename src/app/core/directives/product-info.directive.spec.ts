import { ProductInfoDirective } from './product-info.directive';

describe('ProductInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductInfoDirective();
    expect(directive).toBeTruthy();
  });
});
